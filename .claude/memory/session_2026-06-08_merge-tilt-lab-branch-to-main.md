---
name: Merge tilt-lab/sidecoach-intent branch to main
description: feat/tilt-lab-embed-sidecoach-intent fast-forwarded into main; not yet pushed
type: project
---

Merged `feat/tilt-lab-embed-sidecoach-intent` into `main` (Jonah, 2026-06-08).

- Committed pending beat files first (modified MEMORY.md/MEMORY-archive.md + 11 untracked session beats) onto the feature branch as `chore(beats)`, so they would not dangle across the checkout.
- Merge was a clean fast-forward (linear history, no merge commit, no conflicts).
- `main` is now ahead of `origin/main` by 5 commits. NOT pushed (user did not ask).
- Branch content: tilt-lab portable embed export + open-tiltlab skill, sidecoach intent-tier triggering/cooldown, justify tooltip+launcher overhaul + source guard, retune-port docs.

Files touched: git state only (no source edits this session).
