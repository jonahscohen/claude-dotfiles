---
name: Manipulate - revert preview on task-clear + Fill/Border/Shadow/Filters report
description: Discard a queued Manipulate task -> revert its live preview to original; plus Jonah's report that Fill/Border/Shadow/Filters don't work
type: project
relates_to: [session_2026-06-07_manipulate-highlight-recolor-tooltip.md, session_2026-06-06_retune-build-progress.md]
---

**REVERT-ON-CLEAR (Jonah: "clear task -> styles not restored, shouldn't have to refresh") - FIXED robustly (bundled, pending Jonah reload+verify):**
Manipulate edit = preview.applyChange (live !important sheet via core PreviewEngine) + changeBuffer.add + syncQueue (one _changeQueue task per selector, tagged _manipulate). Discarding a task previously left the preview applied (only a page refresh cleaned it). PreviewEngine (core/preview-engine.ts) already had removeChange(sel,prop) + clearAll(); manipulate's preview/changeBuffer ARE core's instances; core.manipulateMode exists; `(promptMode as any)._changeQueue === core._changeQueue`.
Discard paths found: (1) per-task Remove -> prompt _confirmRemoveItem splice (1425); (2) queue panel "Clear All" btn -> `_changeQueue.length=0` (1276) [NEITHER reverted]; (3) Send All (1338) = after-send, correctly NO revert; (4) toolbar core.clearAll (782) already reverts via previewEngine.clearAll + switchMode(null). deactivate() does NOT revert (correct - queued tasks persist for later send).
FIX: core.revertManipulatePreview(sel?) - sel reverts one selector, omitted reverts all; delegates to manipulateMode.revertSelector/revertAll when active, else falls back to core previewEngine.removeChange/clearAll + changeBuffer (robust to mode teardown). ManipulateMode.revertSelector(sel) (CSS removeChange + buffer.remove + restore original attributes from attrOriginals + renderPanel if active selector) and revertAll(). attrOriginals map captures the true original attr value once (handleAttributeChange). Wired: _confirmRemoveItem -> revertManipulatePreview(sel); Clear All btn -> revertManipulatePreview() when any _manipulate task present.
NOTE: Jonah's "still not restored" report arrived seconds after the FIRST (per-task-only) install - he was almost certainly on the pre-fix core. Reload required.

**FILL / BORDER / SHADOW / FILTERS report (Jonah: "not included yet or not working, make sure I can use these") - INVESTIGATING, root cause unknown:**
Verified the sections DO emit correct props: FillSection -> backgroundColor/backgroundImage (+opacity/zIndex/radius/overflow); BorderSection -> borderWidth/Style/Color; ShadowSection -> boxShadow (shadowToCss); FiltersSection -> filter/backdropFilter. PropertyPanel renders all four (lines 293-297) and passes onPropertyChange (base.onPropertyChange = handlePropertyChange, real not no-op). So the wiring exists - failure is more specific. ROOT-CAUSED via Jonah's symptom ("click Fill -> object gets white fill but NO controls appear to adjust it; sections just show '+' headers"): the panel is NOT reactive. handlePropertyChange applied preview + buffer + syncQueue but NEVER updated this.scopedStyles (the `s` prop, a selection-time snapshot) and NEVER re-rendered. Fill/Border/Shadow/Filters gate their controls on `s.*` (FillSection hasFill from s.backgroundColor; ShadowSection hasShadow = !!s.boxShadow && !=="none"; FiltersSection state from parseFilters(s.filter,...) synced on s.filter change; Border similar). So clicking "+" applied the change to the page but hasFill/hasShadow/etc. stayed false -> controls never revealed. FIX (1 line in handlePropertyChange): `this.scopedStyles = { ...this.scopedStyles, [prop]: value }; this.renderPanel();` before syncQueue. Now the panel reflects every change -> "+" reveals controls, control values stay current. WATCH (ask Jonah to verify): re-rendering on every change could disrupt input focus / number-scrub / the color FloatingDialog - Preact reconciliation SHOULD preserve focus, but confirm; if janky, debounce renderPanel or re-render only on gating-relevant changes. (handleAttributeChange intentionally left without renderPanel - ImageSection applies attrs to the DOM itself + manages own state; revisit if attribute-gated UI has the same reveal issue.)

**VERIFICATION GAP (recurring):** after a reload the Justify toolbar will not re-activate in the agent's automated browser session (POST /activate returns ok but no toolbar; cmd+shift+period key rejected). All fixes this session ship "served-core-confirmed" + Jonah verifies in his live session. Resolving activation would let the agent self-verify.

**FOLLOW-UPS (Jonah after reveal fix): (1) Border still broken; (2) clear should reset the PANEL options, not just the live element - DONE in source (pending reload+verify):**
The naive `scopedStyles = {...scopedStyles, [prop]: value}` reveal fix worked for Fill/Shadow/Filters (gating reads the SAME key that's set) but NOT Border: handleAddBorder emits SHORTHANDS (borderWidth/borderStyle/borderColor) while hasBorder + controls read LONGHANDS (s.borderTopWidth/borderTopStyle/...). Setting scopedStyles.borderWidth doesn't touch the longhand keys -> hasBorder stays false -> never reveals.
FIX (unifies both): replaced naive set with `refreshScopedStyles()` = re-read getComputedStyle(selectedElement) for every scopedStyles key (camelToKebab -> getPropertyValue). getComputedStyle reflects the preview's adopted !important sheet AND expands shorthands to longhands, so Border reveals + all sections stay accurate. Called in handlePropertyChange (after applyChange), revertSelector (after removeChange -> resets panel to original), and revertAll. This also fixes #2: after the reveal fix scopedStyles held the CHANGED values, so reverting the preview alone left the panel showing the change; now revert re-reads original computed styles + renderPanel, so the panel controls reset (Fill collapses back to "+", etc.). Tradeoff: switches from scope-aware getScopedStyles to raw getComputedStyle on edit (effective current value - correct for live edit; minor fidelity shift possible). Perf: one getComputedStyle + N getPropertyValue per change; watch scrub jank.

**REGRESSION + CORRECT FIX (Jonah: "Fill/Border/Shadow no longer work. Why isn't this 1:1 with retune?"):**
My getComputedStyle-refresh attempt introduced a DUPLICATE method `refreshScopedStyles` (one already existed at line ~213, getScopedStyles-based, also sets ownedProperties). JS class: later def wins -> my version silently REPLACED the real one, dropped ownedProperties, AND was called by the EXISTING call sites (selectElement/scope-change at ~180/250) -> broke selection-time scopedStyles -> Fill/Border/Shadow all broke. Plus getScopedStyles itself CANNOT see the preview: it walks CSSOM stylesheet rules (walkScopedRules over document.styleSheets), and the preview is an adoptedStyleSheet (NOT in document.styleSheets) -> re-reading it after an edit returns stale pre-edit values.
CORRECT FIX: (a) deleted the duplicate method (restored the original refreshScopedStyles as sole def -> un-breaks 180/250/selection). (b) handlePropertyChange now MERGES the change directly into scopedStyles + renderPanel (the working naive approach for Fill/Shadow/Filters) AND expands border SHORTHANDS to longhands (borderWidth/Style/Color -> border-{Top,Right,Bottom,Left}-*) so Border's longhand gating reveals. (c) revertSelector/revertAll call the original refreshScopedStyles AFTER preview.removeChange/clearAll - the preview is GONE so getScopedStyles reads the true original -> panel controls reset to original (Fill collapses to "+").
1:1 ANSWER (told Jonah): the panel/controls/11 sections ARE 1:1 from Retune; the INTEGRATION SPINE (edit -> panel update) is NOT - Retune drives sections from a reactive store its sections assume; our port wired a custom minimal spine (preview+buffer+queue) that didn't feed the panel, because Retune's store is bolted to its engine/React-context (+ paid-icon overlay we can't run). These section reveal/revert bugs are all that one gap. The merge-into-scopedStyles + renderPanel is our re-implementation of Retune's reactive update.

**PER-CONTROL RESET -> QUEUE SYNC (Jonah: "reset a property -> the change should leave the task; empty task -> remove task"):** changeProps.onReset already did preview.removeChange + buffer.remove + syncQueue + renderPanel (syncQueue rebuilds the queue from the buffer, drops the change, and skips a selector with no changes -> empty task removed; _showQueuePill hides the pill at length 0). Gaps fixed: (a) onReset didn't restore scopedStyles[prop] so the reset CONTROL still showed the changed value (looked broken) -> now calls mergeScopedStyle(prop, c.oldValue) to reset the control to original (without nuking other pending edits); (b) an OPEN queuebar panel didn't live-refresh -> added core._refreshOpenQueuePanel() (remove panel + _toggleQueuePanel rebuild, mirrors per-task-remove), called from onReset after syncQueue. Also extracted mergeScopedStyle(prop,value) helper (merge + border shorthand->longhand expansion) shared by handlePropertyChange and onReset.

**MINUS BUTTON -> revert task + reset live view (Jonah):** the section "-" (handleRemoveFill etc.) emits onPropertyChange("backgroundColor","transparent")+("backgroundImage","none") i.e. ADDS changes rather than removing the pending add. FIX (general, handles "-" AND manual back-to-original): snapshot `originalComputed` (getComputedStyle, normalized) at selectElement; in handlePropertyChange, after preview.applyChange, read getComputedStyle(el)[kebab] and if it === originalComputed[prop] the edit returns the property to its original -> treat as REVERT (preview.removeChange + buffer.remove + mergeScopedStyle(prop,value) + syncQueue + _refreshOpenQueuePanel + return), so the task drops it (removed when empty) and the live view resets. getComputedStyle DOES reflect the preview's adopted sheet (the element visibly changes), and normalizes formats so transparent===rgba(0,0,0,0).

**PANEL UI -> match Justify chrome (Jonah):** (1) background: panel-shell.css dark `--retune-surface` color-mix(...) -> `#1a1a1a` (matches the claudebar/queuebar flat #1a1a1a). (2) fonts: `:host` base InterVariable -> `JustifySans` (cascades via `font-family:inherit` on controls); JustifyMono on the value/data fields - controls.css .retune-prop-input + .retune-text-input-field + .retune-slider-value, color-gradient.css .retune-color-hex-input + .retune-color-opacity-input, sections.css .retune-selector-tag-count (the count badge); JustifySans on .retune-selector-tag-name + .retune-floating-dialog (was stray 'Inter'). 0 Inter left in core/manipulate/styles. Fonts are @font-face'd into document.head (core/index.ts:228) so the shadow panel resolves them.

**DROPDOWN OPEN -> toolbar/panel jumps right (Jonah):** useScrollLock (core/manipulate/ui/use-scroll-lock.ts) used Retune's `overflow:hidden` + `padding-right=scrollbarWidth`. The padding fixes page CONTENT shift but NOT fixed-position elements: hiding the scrollbar widens the viewport/ICB, so the fixed right-anchored toolbar+panel jump right by the scrollbar width on every dropdown/dialog open. FIX (deviation from verbatim Retune): reserve the gutter with `scrollbar-gutter: stable` on documentElement (set before overflow:hidden, restored on unlock) so the viewport width is unchanged -> nothing moves. Dropped the padding-right compensation. Chrome 94+.

Collaborator: Jonah. Build/serve: node build.js -> dist/justify-core.js; cp to ~/.claude/justify/dist/; daemon serves per-request no-store; reload to pull.
